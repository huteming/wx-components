<view class="{{ space ? 'c-button' : '' }}">
  <button
    class="c-button_{{ type }}"
    size="{{ size }}"
    plain="{{ plain }}"
    disabled="{{ loading || disabled }}"
    loading="{{ loading }}"
    bindtap="handleClick">
    <slot />
  </button>
</view>
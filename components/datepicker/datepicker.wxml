<wxs module="filter" src="./datepicker.wxs" />

<picker
  mode="{{ mode }}"
  value="{{ value }}"
  start="{{ start }}"
  end="{{ end }}"
  bindchange="handleChange">
  <view class="c-datepicker-select">{{ filter.empty(value) }}</view>
</picker>